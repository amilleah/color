[project]
name = "color"
version = "0.1.0"
description = "tools for building a color archive"
requires-python = ">=3.9"
dependencies = [
    "matplotlib>=3.8",
    "numpy>=1.26",
    "pillow>=10.0",
    "scikit-learn>=1.4",
]
